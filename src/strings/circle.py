import math

radius = float(input("enter the radius of the circle : "))

circumference = 2 * math.pi * radius

print(f" the circumference of the circle is {round(circumference, 2)}") #here i rounded the circumference to 2 decimal places